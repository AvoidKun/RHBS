<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.avoid.phbs.mapper.AnnouncementMapper">
    <!--动态sql-->
    <select id="list" resultType="com.avoid.phbs.model.Announcement">
        select * from announcement
        <where>
            <if test="announcementCategoryId!=null">
                announcement_category_id=#{announcementCategoryId}
            </if>
            <if test="state!=null">
                and state=#{state}
            </if>

            and create_admin=#{adminId}
        </where>
    </select>
    <select id="findEvalution" resultType="com.avoid.phbs.model.Evaluation">
        select * from evaluation
        <where>
            <if test="state!=null">
                and state=#{state}
            </if>
        </where>
    </select>
</mapper>